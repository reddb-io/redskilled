---
"@reddb-io/red-skills": patch
---

Ship the pinned Redskilled system tray runtime inside the signed workstation package set, provision it without npm at daemon startup, and activate the daemon and dashboard services during installation.
