// The neighbour that makes `fixture-badge`'s shared idiom shared. See
// `fixture-badge.variants.ts` for what the pair is for.

import { tv } from "tailwind-variants";

export const fixtureChip = tv({
  base: "fixture-chip-cap inline-flex rounded-full border",
});
