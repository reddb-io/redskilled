// The same application, importing one Primitive instead. Its bundle is what
// makes the other one's evidence readable: the behavior base has to be absent
// from somewhere for its presence to mean anything.

import { Button } from "@reddb-io/design-system/base";

export default Button;
