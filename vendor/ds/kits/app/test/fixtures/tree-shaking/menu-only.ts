// An application that imports one inherited Base Composite and nothing else.
//
// Committed rather than generated, because it is the input to a claim about
// the build: the entry a consumer writes, from the canonical public subpath,
// so what the bundler sees here is what it sees out there.

import { DropdownMenu } from "@reddb-io/design-system/base";

export default DropdownMenu;
