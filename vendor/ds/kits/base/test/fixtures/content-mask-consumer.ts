// ContentMask contracts through the Base subpath a Product Application
// consumes. Private source files cannot satisfy these tests.
export {
  CONTENT_MASK_CLIP_PATHS,
  CONTENT_MASK_SHAPES,
  ContentMask,
  contentMask,
  type ContentMaskShape,
  type ContentMaskVariants,
} from "@reddb-io/design-system/base";
